# @package hydra
run:
  dir: eaif_results/${hydra.job.name}/${oc.env:USER}/${WANDB.name}
  subdir: ${hydra.job.num}_${now:%Y-%m-%d}_${now:%H-%M-%S}
sweep:
  dir: eaif_results/${hydra.job.name}/${oc.env:USER}/${WANDB.name}
  subdir: ${hydra.job.num}_${now:%Y-%m-%d}_${now:%H-%M-%S}
